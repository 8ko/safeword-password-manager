import React from "react";

import Toolbar from '@mui/material/Toolbar';

export default function Vault() {
    return (
        <div>
            <Toolbar />
            <h2>Vault</h2>
        </div>
    );
}